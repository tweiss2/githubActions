name: First Workflow
on: workflow_dispatch # can manually trigger workflow
jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: echo "Hello World!"
      - name: Multi-line shell Command
        run: |
            echo "First Output"
            echo "Second Output"
      - name: Print goodbye
        run: echo "goodbye!"
